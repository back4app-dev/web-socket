{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "A WebSocket server with an HTTP health check endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.5.0"
  }
}
